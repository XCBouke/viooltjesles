@* ~/_PageStart.cshtml *@
@{
    PageData["SecretMessage"] = "Alleen voor leden!";
    if (!WebSecurity.IsAuthenticated)
    {   
        string returnUrl = Request.RawUrl;
        Response.Redirect("~/Account/Login?ReturnUrl=" + returnUrl);
    }
}
